apiVersion: nullxes.model-stage/v2
kind: ProxyStage
metadata: {id: PX2-7B, status: planned, execution_state: not_run}
spec:
  lane: proxy
  parent_weights: none
  initialization: from_scratch
  dependencies: [PX1-1B]
  scale: {nominal_parameters: 7B, exact_parameters: PENDING_PROXY_SPEC}
  purpose: "Measure dense/GQA optimization, data scaling and μP transfer candidates."
  inputs:
    tokenizer: artifact://latex-e01/T0-TOKENIZER/PENDING
    corpus: artifact://latex-e01/D0-CORPUS/PENDING
  transfers_to_target: [scaling_fit, optimizer_hypotheses, initialization_hypotheses]
  forbidden_transfers_to_target: [weights, optimizer_state, checkpoint_shards]
  runtime: {accelerator: NVIDIA_H200, local_execution: forbidden}
  gates: [PX1_accepted, scaling_fit_recorded, exact_resume_pass, no_target_weight_parentage]
  output: artifact://latex-e01/proxy/PX2-7B/PENDING
