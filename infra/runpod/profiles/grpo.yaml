schema: laetex.runpod.profile/v3
name: grpo
enabled: true
stage: GRPO
classification: EXPERIMENT_REQUIRED
purpose: "Executable-environment GRPO from PREF with verifier-authoritative reward."
platform: {provider: RunPod, cloud: Secure Cloud, accelerator: NVIDIA H200, local_model_workloads: forbidden}
compute: {minimum: {world_size: 512, nodes: 64}, recommended: {world_size: 1024, nodes: 128}, precision: BF16}
parallelism:
  minimum: {TP: 8, PP: 8, EP: 8, CP: 1, DP: 1}
  recommended: {TP: 8, PP: 8, EP: 16, CP: 1, DP: 1}
  rule: "world_size=TP*PP*EP*CP*DP"
  rollout_partition: "Recorded separately; rollout+update ranks must equal world_size."
network: {required: "provider-attested NVLink/NVSwitch and InfiniBand", acceptance: "all-to-all, rollout/update sync, reward replay and sandbox containment"}
storage: {working: "RunPod Network Volume for transient rollout queue", durable_source_of_truth: "external encrypted versioned object store"}
checkpoint: {input: "PREF BF16 and immutable sandbox tasks", output: "GRPO BF16 full state and accepted trajectories", cadence: "measured time/update interval", cold_restore_required: true}
wall_clock: {classification: ENGINEERING_HYPOTHESIS, estimate: "TBD; sandbox throughput may dominate"}
gate: "positive lower VETCR bound, recovery gain, unsafe effects=0 and reward hacking=0"
economic_gate: "Proceed only if a 512-H200 pilot beats additional verified SFT per incremental completion."
